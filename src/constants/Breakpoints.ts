// Shared breakpoints for responsive layout. Widths below `tablet` are
// mobile, [`tablet`, `desktop`) is tablet/half-screen, and >= `desktop`
// is desktop/full-screen.
export const BREAKPOINTS = {
  mobile: 0,
  tablet: 600,
  desktop: 1024,
};
